# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local
*.tsbuildinfo

# The self-contained build is committed so the app can be opened with no
# server and no build step — but ignore any other generated build output.
dist/*
!dist/index.html
!dist/favicon.svg

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Inspection artefacts from tools/cutout.py (regenerate locally)
src/assets/scene/_check_*
src/assets/scene/_grid_*

# Local screenshot scratch
_*.png
